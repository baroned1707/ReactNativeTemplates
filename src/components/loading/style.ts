import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  loadingContainer: {
    width: '100%',
    height: '100%',
    position: 'absolute',
    zIndex: 15,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'rgba(176, 177, 181, 0.3)',
  },
});
